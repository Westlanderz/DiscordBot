class ConfigMeta {
    private type;

    constructor(type) {
        this.type = type;
    }

    private call(module_name: string, unique_id: string, force_registration: boolean = false, ) {
        
    }
}